# =============================================================================
# JagahVA Bot Environment Configuration
# =============================================================================

# Required: Your Telegram Bot Token
# Get this from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# Required: Your Telegram User ID
# Get this by sending /start to @userinfobot on Telegram
ALLOWED_USER_ID=your-telegram-user-id-here

# =============================================================================
# Database Configuration
# =============================================================================

# Optional: MongoDB Atlas Connection String
# Get this from MongoDB Atlas dashboard
# Format: mongodb+srv://username:password@cluster.mongodb.net/jagahva?retryWrites=true&w=majority
# If not set, bot will use local db.json file
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/jagahva?retryWrites=true&w=majority

# =============================================================================
# Server Configuration
# =============================================================================

# Optional: Web server port (default: 3000)
PORT=3000

# Optional: Environment (development/production)
# Set to 'production' for Render deployment
NODE_ENV=production

# Optional: Render external URL (for webhook support)
# Set this in Render dashboard for webhook functionality
RENDER_EXTERNAL_URL=https://your-app-name.onrender.com

# =============================================================================
# Bot Configuration
# =============================================================================

# Optional: Custom reminder time (24h format, default: 20:00)
REMINDER_TIME=20:00

# Optional: Timezone (default: GMT)
TIMEZONE=GMT

# Optional: Debug mode (set to true for verbose logging)
DEBUG=false

# =============================================================================
# Optional: OpenAI Integration (not required - bot uses regex parsing)
# =============================================================================

# Optional: OpenAI API Key (for future AI features)
# OPENAI_API_KEY=your-openai-api-key-here 